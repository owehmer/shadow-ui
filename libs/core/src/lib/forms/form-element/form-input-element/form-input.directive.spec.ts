import { SdwFormInputDirective } from './form-input.directive';

describe('SdwFormInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SdwFormInputDirective();
    expect(directive).toBeTruthy();
  });
});
