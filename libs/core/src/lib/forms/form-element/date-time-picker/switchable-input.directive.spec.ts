import { SwitchableInputDirective } from './switchable-input.directive';

describe('SwitchableInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchableInputDirective();
    expect(directive).toBeTruthy();
  });
});
